services:
  api:
    container_name: triageguard-api
    build:
      context: .
      dockerfile: Dockerfile
    image: triageguard-api:latest
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
